package com.lonaloto.ui.theme

import androidx.compose.ui.graphics.Color

/**
 * Palette LONALOTO — inspirée des couleurs nationales ivoiriennes (orange/vert),
 * cohérente avec le dégradé du logo de l'app de référence Sygis-Primaire.
 *
 * ⚠️ Palette de départ, centralisée ici volontairement : si un vrai logo/charte
 * graphique LONALOTO est fourni, il suffit de modifier ce seul fichier pour que
 * toute l'app change de couleurs (aucun autre fichier à toucher).
 */

// Orange — couleur primaire (boutons d'action, accents forts)
val OrangeLonaloto = Color(0xFFF77F00)
val OrangeLonalotoFonce = Color(0xFFC96400)
val OrangeLonalotoClair = Color(0xFFFFE0B2)

// Vert — couleur secondaire (validation, succès, montants positifs)
val VertLonaloto = Color(0xFF1B8A4C)
val VertLonalotoFonce = Color(0xFF0F5C31)
val VertLonalotoClair = Color(0xFFC8E6C9)

// Neutres
val FondClair = Color(0xFFFFFBF5)
val FondSombre = Color(0xFF1C1B18)
val SurfaceClair = Color(0xFFFFFFFF)
val SurfaceSombre = Color(0xFF2A2822)
val TexteClair = Color(0xFF1C1B18)
val TexteSombre = Color(0xFFEDE8DE)

// Erreurs (indépendant de la marque, rouge standard Material)
val RougeErreur = Color(0xFFBA1A1A)
